# Samme DATABASE_URL som packages/db — samme PostgreSQL-instans.
# Isolasjonen skjer via postgres-schema 'varelager' (se schema.prisma).
DATABASE_URL="postgresql://user:password@localhost:5432/sitedoc"
