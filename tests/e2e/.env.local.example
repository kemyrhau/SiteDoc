# Kopier til tests/e2e/.env.local (gitignored) og fyll inn.
# DEV_LOGIN_SECRET = samme verdi som DEV_LOGIN_SECRET på sitedoc-test-api.
# Committes ALDRI — bor i server-.env.
DEV_LOGIN_SECRET=

# Valgfritt — standard peker mot test-miljøet.
# E2E_BASE_URL=https://test.sitedoc.no
# E2E_API_URL=https://api-test.sitedoc.no
